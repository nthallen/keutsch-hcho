Experiment=CH2O
hcho=10.245.83.9
FlightNode=$hcho
HomeDir=/home/CH2O
RunLog=saverun.log
Dircksum=no
if [ $(hostname) = ch2o ]; then
  # Leave this commented to disable startup on boot
  # RUNFILE=interact
  LOOP_ON_FILE=$RunLog
  if [ -f saverun.log ]; then
    RunType=raw/data
  else
    RunType=ask
  fi
  Extractions=
  Analysis='nightly_process -q $directory -p'
else
  RunType=ask
  Extractions="hchoengext BCtrext"
fi
